enum ContentSort { newest, oldest }

enum EpisodeFeedFilter { all, unplayed, inProgress, saved, downloaded }

enum PodcastEpisodeFilter { newEpisodes, inProgress, all }

enum ArticleFeedFilter { all, unread, saved }
